#include <iostream>
#include <algorithm>
#include <tuple>
#include <vector>
#include <string>
#include <queue>
#include <cmath>
#include <iomanip>

using namespace std;

int main()
{
    int n;
    cin >> n;
    char a[n][n];
    vector<vector<bool>> ex(n, vector<bool>(26, false));
    for(int i = 0; i<n;i++){
        for(int j = 0; j<n; j++){
            cin >> a[i][j];
            ex[i][]
        }
    }
    return 0;
}